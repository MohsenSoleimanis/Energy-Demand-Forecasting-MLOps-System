api:
  host: "0.0.0.0"
  port: 8000
  workers: 4
  model_name: "energy-demand-forecaster"
  model_stage: "Production"
  batch_size: 1000
  timeout: 30
  cors_origins:
    - "http://localhost:3000"
    - "http://localhost:8080"

logging:
  level: "INFO"
  format: "json"
